stages:
  - install
  - build

# Definimos un trabajo para la etapa de build
install_job:
  stage: install
  script:
    - echo "isntall dependecias..."
    - npm install
# Definimos un trabajo para la etapa de build
build_job:
  stage: build
  script:
    - echo "Compilando la aplicación..."
    - npm run build
